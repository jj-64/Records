# -*- coding: utf-8 -*-
"""
import pandas as pd
import pandas_datareader.data as web
import datetime
quit
quit
series_single <- yearly_avg_unrate %>% filter(year <= 2000)
quit
plot(y = series_single$average, x = series_single$year)
quit
abline(lm(series_single$average ~ series_single$year))
quit
test_data(series_single$average)
quit
quit
series_single <- yearly_avg_unrate %>% filter(year <= 2000)
quit
plot(y = series_single$average, x = series_single$year)
quit
abline(lm(series_single$average ~ series_single$year))
quit
test_data(series_single$average)
quit
quit
quit
quit
quit
series_single <- yearly_avg_unrate %>% filter(year <= 2000)
quit
plot(y = series_single$average, x = series_single$year)
quit
abline(lm(series_single$average ~ series_single$year))
quit
test_data(series_single$average)
quit
View(series_single)
quit
quit
quit
quit
## best metrics (mtry...)
quit
best <- tune::select_best(results$rf, metric = "roc_auc")
quit
final <- tune::finalize_workflow(
quit
workflows$rf,
quit
best
quit
)
quit
## rerun the model on the train data
quit
final_fit <- iml::fit(final, data = train_data)
quit
