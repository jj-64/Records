% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Likelihood_Xt.R
\name{Likelihood_Xt_iid}
\alias{Likelihood_Xt_iid}
\title{Compute Log-Likelihood for IID Frechet Model}
\usage{
Likelihood_Xt_iid(x, dist = c("frechet", "norm"), params)
}
\arguments{
\item{x}{Numeric vector. Observed data.}

\item{dist}{Character, distribution name. One of:
"norm", "frechet"}

\item{params}{list of vector of parameter:
\itemize{
\item Frechet: scale and shape.
\item Norm: mean and sd.
}}
}
\value{
Numeric value of the log-likelihood.
}
\description{
Computes the log-likelihood of independent and identically distributed (IID)
variables based on full information of the series, and under different
underlying distribution.
}
\details{
For \code{Frechet} distribution, use the \strong{VGAM} package to compute:
\deqn{llog L = \sum \log(f_{\text{Frechet}}(x; scale shape))}

For \code{Normal} distribution, use the \strong{dnorm} function:
\deqn{log L = \sum \log(f_{\text{Normal}}(x; \mu, \sigma))}
}
\examples{
Xt = VGAM::rfrechet(n = 100, shape = 1, scale = 2)
Likelihood_Xt_iid(x = Xt, dist = "frechet", params =  list(shape = 1, scale=2))
Likelihood_Xt_iid(x = Xt, dist = "norm", params =  list(mean = mean(X), sd= sd(X) ))
}
