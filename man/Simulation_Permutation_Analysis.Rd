% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Test_Permutation.R
\name{Simulation_Permutation_Analysis}
\alias{Simulation_Permutation_Analysis}
\title{Monte Carlo Simulation of Record Model Identification via Test Permutations}
\usage{
Simulation_Permutation_Analysis(
  n_sim = 1000,
  T = 50,
  generator,
  series_args = list(),
  n_arg = "T",
  H0 = c("DTRW", "LDM", "Classical", "YNM"),
  alpha = 0.05,
  lag = 10,
  warmup = 2,
  print = FALSE,
  info = "All",
  approximate = FALSE,
  one.sided = FALSE
)
}
\arguments{
\item{n_sim}{Number of simulated series (default = 1000)}

\item{T}{Length of each time series (default = 50)}

\item{generator}{String. The name of series generator}

\item{series_args}{List. Arguments of the generator of the series, the
argument "T" is not included}

\item{n_arg}{Character. default is "T" as some generator functions take "n"}

\item{H0}{Character. The true generating process: "DTRW", "LDM",
"Classical", or "YNM"}

\item{alpha}{Numeric. Significance level for all tests (default = 0.05)}

\item{lag}{Integer. Lag parameter for Box-Jenkins test (default = 10)}

\item{warmup}{Integer. Warm-up parameter for YNM test (default = 2)}

\item{print}{logical default is FALSE, summary is not printed}

\item{info}{String. "All" if data provided is the whole series \eqn{X_t} or
"Records" if the underlying series is \eqn{R_n}. In this case, the parameter
record_times must be provided.
Forced in case "info" = "Records"}

\item{approximate}{Logical, if \code{TRUE} use the asymptotic normal approximation for DTRW \eqn{N_T}-test
(default = \code{FALSE} for the exact quantile test).}

\item{one.sided}{Logical, if \code{TRUE} perform a one-sided test for DTRW \eqn{N_T}-test
(default = \code{FALSE} for two-sided).}
}
\value{
A list containing:
\describe{
\item{results_all}{Data frame of all simulation × permutation outcomes}
\item{summary_total}{Frequency of accepted decisions across all runs}
\item{perm_dec_table}{Contingency Table of waht each permutation returns}
\item{accuracy_by_perm}{How often each permutation recovered the true H0}
}
}
\description{
Runs multiple simulations of time series generated under a specified null model (e.g. DTRW, LDM, iid, YNM),
applies all 24 possible permutations of four record-based tests (DTRW, LDM, Classical, YNM),
and records which test (if any) is accepted by each permutation.
}
